This is a nodejs project which renders react js app with some other routes(can be found in server.js file)
The react project is contained in eurowings-digital folder.

For running the app:
Step 1. npm i in the root folder here
Step 2. npm start

Here npm start takes care of installing dependencies in react project and creating a production build apart from running node server and serving the react build.